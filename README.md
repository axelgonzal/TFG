# TFG



En el siguiente GIT está subido cada código usado del modelo (importado directamente desde Google Colab), así como figuras, los modelos entrenados y los resultados en tablas. 

También está subido el código usado para el preprocesamiento de datos.

